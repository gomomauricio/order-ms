spring: 
  application:
      name: order-ms-mgm

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password

  h2:
    console:
      enabled: true
 
  kafka:
    bootstrap-servers: localhost:9092
 

server:
  port: 8081

 

